# {{ .ProjectName }} - A new tool in the toolbox

## Installation

curl -L  | sh

## Installation

## Install Script

Download `{{ .ProjectName }}` and install into a local bin directory.

#### MacOS, Linux, WSL

Latest version:

```bash
curl -L https://raw.githubusercontent.com/{{ .GithubRepo }}/main/generated/install.sh | sh
```

Specific version:

```bash
curl -L https://raw.githubusercontent.com/{{ .GithubRepo }}/main/generated/install.sh | sh -s 0.0.4
```

## Manual download

Get the archive that fits your system from the [Releases](https://github.com/{{ .GithubRepo }}/releases) page and
extract the binary into a folder that is mentioned in your `$PATH` variable.
